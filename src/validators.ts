   

   import {z} from "zod"

   export const userSchema = z.object({
   //  user_id: z.number().default(0),
   //  full_name: z.string(),
   //  CardNumbers: z.number(),
   //  month:z.number(),
   //  Year:z.number(),
   //  CVV:z.number()
   })